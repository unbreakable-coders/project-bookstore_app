🧭 Unbreakable Coders — Git Workflow & Branch Strategy

Цей документ описує єдиний, стандартизований робочий процес, якого дотримується вся команда.
Він включає правила гілок, робочий цикл розробника, коміт-меседжі, PR-процес, релізи та рульсет.

🌱 1. Основні гілки
🔵 main

релізна гілка

містить тільки стабільний, протестований код

захищена (no direct push, no force push, require PR)

🟢 develop

головна робоча гілка

сюди зливаються всі задачі

захищена від прямих пушів

PR only

🚀 2. Базовий робочий цикл розробника
1️⃣ Оновити локальний develop
git checkout develop
git pull origin develop

2️⃣ Створити гілку під Jira-задачу

Формат:

<JIRA_ID>_<короткий-опис>


Приклад:

git checkout -b DEV-7_add-header

3️⃣ Виконати задачу + робити коміти

(див. розділ про Conventional Commits)

4️⃣ Запушити гілку
git push origin DEV-7_add-header

5️⃣ Створити Pull Request

обрана гілка → develop
compare → DEV-7_add-header

Назва PR:

feat(DEV-7): add header

6️⃣ Пройти ревʼю → внести правки → отримати approve
7️⃣ Змерджити PR у develop та видалити гілку
🧩 3. Conventional Commits
Формат:
<type>(<JIRA_ID>): <короткий опис>

Приклади:
feat(DEV-7): add header layout
fix(DEV-12): fix login validation
refactor(DEV-15): simplify card component
style(DEV-20): update button styles
docs(DEV-3): add project setup instructions
chore(DEV-1): configure eslint and prettier

Типи комітів:
Тип	Опис
feat	новий функціонал
fix	виправлення багів
refactor	зміни без нового функціоналу
style	стилі, форматування
docs	документація
test	тести
chore	технічні зміни
🔀 4. Pull Requestʼи
4.1. PR у develop

Кожна Jira-задача → окремий PR.

Назва:
feat(DEV-7): add header

Опис PR повинен містити:

що саме зроблено

як протестувати

важливі деталі / залежності

Обовʼязкові вимоги перед створенням PR:
npm run fix-style
npm run dev

проєкт має запускатися без помилок

не перевищуємо ~500 рядків, якщо можливо!!!

4.2. Ревʼю

мінімум 1 approve

ввічливі, конструктивні коментарі🤞

якщо щось незрозуміло → ставимо питання

🚢 5. Релізи у main

Релізи створює тимлід.

1️⃣ Оновити develop
git checkout develop
git pull origin develop

2️⃣ Створити релізну гілку
git checkout -b release/sprint-1
git push origin release/sprint-1

3️⃣ PR у main

base: main

compare: release/sprint-1

Назва:

release: sprint 1


Опис: короткий список ключових задач.

4️⃣ Після approve → merge у main
5️⃣ Деплой запускається автоматично (GitHub Actions)
🛡️ 6. Branch Protection Rules
6.1. develop

Заборонено:

❌ direct push

❌ force push

Дозволено:

✔ merge через PR

✔ 1 approve (рекомендовано)

✔ required status checks (lint) — коли будуть

6.2. main

Заборонено:

❌ direct push

❌ force push

❌ deletion

Дозволено:

✔ PR ONLY

✔ 1–2 approve

✔ required: lint / build / tests

✔ require linear history (рекомендовано)

⚠️ 7. Конфлікти

Якщо зʼявився конфлікт:

Зберігаємо спокій 🙂

Дивимось конкретні файли

Узгоджуємо рішення з автором або тімлідом

Виправляємо → перевіряємо запуск:

npm run dev


Робимо коміт:

fix: resolve merge conflict in <file>

✔ 8. Чекліст перед пушем

Перед тим, як пушити:

 git status чистий

 запущено npm run fix-style

 проєкт стартує (npm run dev)

 коміт у форматі Conventional Commits

 гілка названа за Jira (DEV-XX_description)

 немає зайвих файлів (temp, dist, node_modules, локальні конфіги)

🏁 Фінал

Цей документ — обовʼязковий стандарт Git-роботи команди.